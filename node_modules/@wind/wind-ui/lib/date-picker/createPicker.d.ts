import moment from 'moment';
export interface PickerProps {
    value?: moment.Moment;
    prefixCls: string;
}
export declare const popupAlign: {
    points: string[];
    overflow: {
        adjustX: number;
        adjustY: number;
    };
    offset: number[];
    targetOffset: number[];
};
export default function createPicker(TheCalendar: any): any;

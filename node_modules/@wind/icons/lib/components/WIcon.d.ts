import * as React from 'react';
import { IconDefinition } from '../svg/types';
import { IconBaseProps } from './Icon';
export interface WIconProps extends IconBaseProps {
}
export interface IconComponentProps extends WIconProps {
    icon: IconDefinition;
}
interface IconBaseComponent<Props> extends React.ForwardRefExoticComponent<Props & React.RefAttributes<HTMLSpanElement>> {
}
declare const Icon: IconBaseComponent<IconComponentProps>;
export default Icon;

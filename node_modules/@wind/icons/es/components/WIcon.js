import _defineProperty from "@babel/runtime/helpers/esm/defineProperty";
import _objectWithoutProperties from "@babel/runtime/helpers/esm/objectWithoutProperties";
import * as React from 'react';
import classNames from 'classnames';
import ReactIcon from './IconBase';
var Icon = React.forwardRef(function (props, ref) {
  var className = props.className,
      icon = props.icon,
      spin = props.spin,
      rotate = props.rotate,
      tabIndex = props.tabIndex,
      onClick = props.onClick,
      restProps = _objectWithoutProperties(props, ["className", "icon", "spin", "rotate", "tabIndex", "onClick"]);

  var classString = classNames('wicon-svg', _defineProperty({}, "wicon-".concat(icon.name, "-").concat(icon.theme && icon.theme.substring(0, 1)), Boolean(icon.name)), className);
  var svgClassString = classNames({
    'wicon-svg-spin': !!spin || icon.name === 'loading'
  });
  var iconTabIndex = tabIndex;

  if (iconTabIndex === undefined && onClick) {
    iconTabIndex = -1;
  }

  var svgStyle = rotate ? {
    msTransform: "rotate(".concat(rotate, "deg)"),
    transform: "rotate(".concat(rotate, "deg)")
  } : undefined; // const [primaryColor, secondaryColor] = normalizeTwoToneColors(twoToneColor);

  return React.createElement("span", Object.assign({
    role: "img",
    "aria-label": icon.name
  }, restProps, {
    ref: ref,
    tabIndex: iconTabIndex,
    onClick: onClick,
    className: classString
  }), React.createElement(ReactIcon, {
    className: svgClassString,
    icon: icon,
    // primaryColor={primaryColor}
    // secondaryColor={secondaryColor}
    style: svgStyle
  }));
});
Icon.displayName = 'WIcon'; // Icon.getTwoToneColor = getTwoToneColor;
// Icon.setTwoToneColor = setTwoToneColor;

export default Icon;
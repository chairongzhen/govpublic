export interface AbstractNode {
    tag: string;
    attrs: {
        [key: string]: string;
    };
    children?: AbstractNode[];
}
export interface IconDefinition {
    name: string;
    theme: ThemeType;
    icon: (() => AbstractNode) | AbstractNode;
}
export declare type ThemeType = 'filled' | 'outlined' | 'twotone' | 'colored';
export declare type ThemeTypeUpperCase = 'F' | 'O' | 'T' | 'C';
